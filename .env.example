# Google Gemini API Configuration (Optional)
# The LLM vision feature is completely optional. The app works fine without it.
# To enable AI-powered image analysis, set your Gemini API key below.

# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-api-key-here

# Optional: Customize the vision model (default: gemini-1.5-flash)
# Options:
#   - gemini-1.5-flash (recommended, fast and cost-effective)
#   - gemini-1.5-pro (more accurate, more expensive)
#   - gemini-pro-vision (legacy)
LLM_VISION_MODEL=gemini-1.5-flash

# Usage Instructions:
# 1. Copy this file to .env (without the .example extension)
# 2. Replace 'your-api-key-here' with your actual Gemini API key
# 3. Make sure .env is in your .gitignore (it should be by default)
# 4. Install the required packages: pip install google-generativeai pillow
# 5. Restart the application

# Note: If no API key is provided, the app will run in manual-only mode
